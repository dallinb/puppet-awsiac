version: 5
defaults:
  datadir: hieradata
  data_hash: yaml_data
hierarchy:
  - name: "Per environment group data"
    path: "environment/%{environment}.yaml"

  - name: "Common data"
    path: "common.yaml"

