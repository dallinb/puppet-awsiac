---
machine:
  environment:
    AWS_REGION: eu-west-2

dependencies:
  cache_directories:
    - "/opt/circleci/.rvm"
    - "/home/ubuntu/.puppetlabs/etc/code/modules"
  override:
    - "bundle install"
    - "librarian-puppet install --path /home/ubuntu/.puppetlabs/etc/code/modules --verbose"

test:
  override:
    - "bundle exec rake release_checks"

compile:
  override:
    - "bundle exec rake build"

deployment:
  init:
    branch: /.*-init/
    commands:
      - "puppet module install --force --verbose pkg/locp-awsiac-0.1.0.tar.gz"
      - "FACTER_ensure=present ./puppet_wrapper.sh apply examples/init.pp --test"
  destroy:
    branch: /.*-destroy/
    commands:
      - "puppet module install --force --verbose pkg/locp-awsiac-0.1.0.tar.gz"
      - "FACTER_ensure=absent ./puppet_wrapper.sh apply examples/init.pp --test"
